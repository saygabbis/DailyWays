# Coloque em /etc/nginx/conf.d/connection-upgrade.conf (uma vez no servidor)
# O map precisa estar no bloco http {}, não dentro de server {}.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
